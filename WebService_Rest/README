Como executar o Web Service?
Simples! Certifique-se de ter instalado o Tomcat 7:
$ sudo apt-get install tomcat7
Faça uma cópia do arquivo de aplicação web 'distribuídos.war' para o seguinte caminho:
/var/lib/tomcat/webapps
	OU
/usr/share/tomcat/webapps
Feito, basta acessar o seu localhost e encontrar a aplicação em execução! http://localhost:8080/distribuidos/

* O diretório Servers guarda as configurações do Tomcat 7 para o Eclipse
* O diretório WebService guarda o projeto java que resultou no arquivo de aplicação web 'distribuídos.war'
